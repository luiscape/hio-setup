language: python
sudo: required
python:
  - "2.7"

# command to install dependencies
install:
  - ./setup.sh

# run coverage + tests
script:
  - coverage report -m
  - ./test.sh

# issue coverage report
after_success:
  - coveralls
  - killall rethinkdb

# for email notifications
notifications:
    email: false