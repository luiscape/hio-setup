language: python
sudo: required
python:
  - "2.7"

# command to install dependencies
install:
  - ./setup.sh

# run coverage + tests
script:
  - ./test.sh

# issue coverage report
after_success:
  - nosetests --with-coverage
  - coveralls
  - killall rethinkdb

# for email notifications
notifications:
    email: false